{
  secrets => ['sekr3t'],       #Mojolicious uses secret passphrases for security features such as signed cookies.
  host_url => "https://localhost/pastes",
  host_path => "/home/www/web/public/pastes",
  max_iter => 100,
  max_filesize => 3221225472,  #3GB upload limit
  run_as => 'www:www',         #user and group to run as
  enable_logging => 1
}
